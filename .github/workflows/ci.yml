on:
  - push
  - workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: paulo-ferraz-oliveira/setup-beam@fix/booleans
        with:
          elixir-version: '1.13.3'
          otp-version: '24.1'
